println(s"Master2CSVrows: ${Master2CSVrows.mkString("\n")}")
println(s"expectedAlertIds: ${expectedAlertIds.mkString(", ")}")

var line2 = Master2_br.readLine()
while (line2 != null) {
  val values = line2.split(",").map(_.trim)
  if (values.length > alertIdIndex && expectedAlertIds.contains(values(alertIdIndex))) {
    Master2CSVrows += line2
  }
  line2 = Master2_br.readLine()
}
