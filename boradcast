latest_files=$(sshpass -p "$password" ssh "$scp_user@$remote_server" "ls -t $output_location | head -2")

for file in $latest_files; do
    # Copy each file
    sshpass -p "$password" scp "$scp_user@$remote_server:$output_location/$file" "$target_path/"
done
